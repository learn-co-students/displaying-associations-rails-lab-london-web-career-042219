<h1>All Artists</h1>

<div>
  <h3><%= pluralize(@artists.count, 'artists') %></h3>
    <ul>
      <% @artists.each do |artist| %>
      <li>
        <%= artist.name + " " + pluralize(artist.songs.count, "songs")%>
      </li>
      <% end %>
    </ul>
</div>
